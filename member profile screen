import 'package:flutter/material.dart';

class MemberProfileScreen extends StatelessWidget {
  static const routeName = 'member-profile-screen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.white,
        title: Center(
          child: Text(
            'Member Profile',
            style: TextStyle(color: Colors.black),
          ),
        ),
        elevation: 5,
      ),
      body: Container(
        color: Colors.white,
        child: Column(
          children: <Widget>[
            SizedBox(
              height: 70,
            ),
            Text(
              'Company Admin',
              style: TextStyle(fontSize: 40, fontWeight: FontWeight.w300),
            ),
            SizedBox(
              height: 70,
            ),
            Container(
              height: 500,
              child: GridView(
                padding: const EdgeInsets.all(25),
                children: [
                  Card(
                    color: Colors.grey.shade100,
                    elevation: 0,
                    child: Center(
                      child: Text('Approval Request',style: TextStyle(color: Colors.black45),),
                    ),
                    shape: RoundedRectangleBorder(
                        borderRadius: BorderRadius.circular(10)),
                  ),
                  Card(
                    color: Colors.grey.shade100,
                    elevation: 0,
                    child: Center(
                      child: Text('Approved List',style: TextStyle(color: Colors.black45),),
                    ),
                    shape: RoundedRectangleBorder(
                        borderRadius: BorderRadius.circular(10)),
                  ),
                  Card(
                    color: Colors.grey.shade100,
                    elevation: 0,
                    child: Center(
                      child: Text('Denied List',style: TextStyle(color: Colors.black45),),
                    ),
                    shape: RoundedRectangleBorder(
                        borderRadius: BorderRadius.circular(10)),
                  ),
                  Card(
                    color: Colors.grey.shade100,
                    elevation: 0,
                    child: Center(
                      child: Text('Edit Info',style: TextStyle(color: Colors.black45),),
                    ),
                    shape: RoundedRectangleBorder(
                        borderRadius: BorderRadius.circular(10)),
                  ),
                  Card(
                    color: Colors.grey.shade100,
                    elevation: 0,
                    child: Center(
                      child: Text('Messages',style: TextStyle(color: Colors.black45),),
                    ),
                    shape: RoundedRectangleBorder(
                        borderRadius: BorderRadius.circular(10)),
                  ),
                  Card(
                    color: Colors.grey.shade100,
                    elevation: 0,
                    child: Center(
                      child: Text('Admins',style: TextStyle(color: Colors.black45),),
                    ),
                    shape: RoundedRectangleBorder(
                        borderRadius: BorderRadius.circular(10)),
                  ),
                ],
                gridDelegate: SliverGridDelegateWithMaxCrossAxisExtent(
                  maxCrossAxisExtent: 200,
                  // ex. device has 500 px widget - then we have 2 items sit side by side
                  childAspectRatio: 3 / 2,
                  // how items should be sized regarding height and width relation
                  // for 200 width you have to have 300 height
                  crossAxisSpacing: 20,
                  mainAxisSpacing: 20,
                ),
              ),
            ),
          ],
        ),
      ),
    );
  }
}
